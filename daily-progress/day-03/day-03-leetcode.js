// День 3: LeetCode Practice - Valid Parentheses
console.log("=== ДЕНЬ 3: VALID PARENTHESES ===");

// ============================================
// ЗАДАЧА: Valid Parentheses
// ============================================

/*
Дана строка s содержащая только символы '(', ')', '{', '}', '[' и ']'.
Определите, является ли входная строка корректной.

Входная строка корректна если:
1. Открытые скобки закрываются скобками того же типа
2. Открытые скобки закрываются в правильном порядке
3. Каждая закрывающая скобка имеет соответствующую открытую скобку того же типа

Примеры:
Input: s = "()"     Output: true
Input: s = "()[]{}" Output: true  
Input: s = "(]"     Output: false
Input: s = "([)]"   Output: false
Input: s = "{[]}"   Output: true
*/

// ============================================
// ШАГ 1: ПОНИМАНИЕ ЗАДАЧИ (5 минут)
// ============================================

console.log("\n--- Шаг 1: Понимание задачи ---");

// ВОПРОС 1: Объясните задачу своими словами
console.log("Задача: проверить правильность расстановки скобок");

// ВОПРОС 2: Какие входные данные?
console.log("Вход: строка со скобками");

// ВОПРОС 3: Что должна вернуть функция?
console.log("Выход: true/false");

// ВОПРОС 4: Какие примеры можете придумать?
const testCases = [
  "()", // true
  "()[]{}", // true
  "(]", // false
  "([)]", // false
  "{[]}", // true
  "", // true (пустая строка)
  "(((", // false
  ")))", // false
];

console.log("Тест кейсы:", testCases);

// ============================================
// ШАГ 2: ПЛАНИРОВАНИЕ (5 минут)
// ============================================

console.log("\n--- Шаг 2: Планирование ---");

// ВОПРОС 1: Какую структуру данных использовать?
console.log("Структура данных: Stack (стек)");

// ВОПРОС 2: Напишите псевдокод
console.log(`
Псевдокод:
1. Создать пустой стек
2. Для каждого символа в строке:
   - Если открывающая скобка → добавить в стек
   - Если закрывающая скобка → проверить соответствие с верхом стека
3. В конце стек должен быть пустым
`);

// ВОПРОС 3: Какая временная сложность?
console.log("Временная сложность: O(n)");
console.log("Пространственная сложность: O(n)");

// ============================================
// ШАГ 3: РЕАЛИЗАЦИЯ (15 минут)
// ============================================

console.log("\n--- Шаг 3: Ваша реализация ---");

// ЗАДАНИЕ: Напишите функцию isValid самостоятельно!
// НЕ СМОТРИТЕ НА РЕШЕНИЕ НИЖЕ!

function isValid(s) {
  // TODO: Ваша реализация здесь
  // Подсказка: используйте массив как стек
  // stack.push() - добавить элемент
  // stack.pop() - убрать последний элемент

  // Ваш код:

  const stack = [];
  const pairs = {
    ")": "(",
    "}": "{",
    "]": "[",
  };

  for (let char of s) {
    // Если открывающая скобка
    if (char === "(" || char === "{" || char === "[") {
      stack.push(char);
    }
    // Если закрывающая скобка
    else if (char === ")" || char === "}" || char === "]") {
      // Проверяем соответствие
      if (stack.length === 0 || stack.pop() !== pairs[char]) {
        return false;
      }
    }
  }

  // Стек должен быть пустым
  return stack.length === 0;
}

// ============================================
// ШАГ 4: ТЕСТИРОВАНИЕ (5 минут)
// ============================================

console.log("\n--- Шаг 4: Тестирование ---");

// Протестируйте вашу функцию:
function testIsValid() {
  const tests = [
    { input: "()", expected: true },
    { input: "()[]{}", expected: true },
    { input: "(]", expected: false },
    { input: "([)]", expected: false },
    { input: "{[]}", expected: true },
    { input: "", expected: true },
    { input: "((", expected: false },
    { input: "))", expected: false },
  ];

  console.log("Запуск тестов...");

  tests.forEach((test, index) => {
    const result = isValid(test.input);
    const passed = result === test.expected;

    console.log(`Тест ${index + 1}: "${test.input}"`);
    console.log(`  Ожидается: ${test.expected}`);
    console.log(`  Получено: ${result}`);
    console.log(`  Результат: ${passed ? "✅ ПРОШЕЛ" : "❌ НЕ ПРОШЕЛ"}`);
    console.log("");
  });
}

// Раскомментируйте когда напишете функцию:
testIsValid();

// ============================================
// РЕШЕНИЕ (СМОТРИТЕ ТОЛЬКО ПОСЛЕ СВОЕЙ ПОПЫТКИ!)
// ============================================

console.log("\n--- Решение (смотрите только после попытки!) ---");

function isValidSolution(s) {
  const stack = [];
  const pairs = {
    ")": "(",
    "}": "{",
    "]": "[",
  };

  for (let char of s) {
    // Если открывающая скобка
    if (char === "(" || char === "{" || char === "[") {
      stack.push(char);
    }
    // Если закрывающая скобка
    else if (char === ")" || char === "}" || char === "]") {
      // Проверяем соответствие
      if (stack.length === 0 || stack.pop() !== pairs[char]) {
        return false;
      }
    }
  }

  // Стек должен быть пустым
  return stack.length === 0;
}

// ============================================
// ДОПОЛНИТЕЛЬНЫЕ ВЫЗОВЫ
// ============================================

console.log("\n--- Дополнительные вызовы ---");

// Вызов 1: Что если есть другие символы?
function isValidExtended(s) {
  // Игнорируем символы, которые не скобки
  const filtered = s.replace(/[^(){}[\]]/g, "");
  return isValidSolution(filtered);
}

console.log("Тест с другими символами:");
console.log('isValidExtended("a(b)c"): ', isValidExtended("a(b)c")); // true

// Вызов 2: Подсчет глубины вложенности
function maxDepth(s) {
  let depth = 0;
  let maxDepth = 0;

  for (let char of s) {
    if (char === "(") {
      depth++;
      maxDepth = Math.max(maxDepth, depth);
    } else if (char === ")") {
      depth--;
    }
  }

  return maxDepth;
}

console.log('Максимальная глубина "((()))":', maxDepth("((()))")); // 3

// ============================================
// ВЫВОДЫ
// ============================================

console.log("\n🎯 ВЫВОДЫ:");
console.log("✅ Stack идеально подходит для проверки соответствий");
console.log("✅ LIFO (Last In, First Out) принцип работы стека");
console.log("✅ Временная сложность O(n) - проходим строку один раз");
console.log(
  "✅ Пространственная сложность O(n) - в худшем случае все символы в стеке"
);

console.log("\n🚀 Готовы к Блоку 2: TypeScript Practice!");
